---
title: "Content — mukoko registry"
description: "Content guidelines for the Mukoko ecosystem: voice, tone, writing style, error messages, and inclusive language."
---

import { ArrowRight, PenTool, AlertTriangle, Heart } from "lucide-react"

# Content

How we write across the Mukoko ecosystem. These guidelines ensure a consistent voice, respectful tone, and clear communication for our pan-African audience.

<div className="mt-8 grid gap-4 sm:grid-cols-3">
  {[
    { title: "Writing Guidelines", href: "/content/writing", description: "Voice, tone, and language conventions", icon: PenTool },
    { title: "Error Messages", href: "/content/error-messages", description: "Patterns for clear, helpful error communication", icon: AlertTriangle },
    { title: "Inclusive Language", href: "/content/inclusive-language", description: "Pan-African context and respectful terminology", icon: Heart },
  ].map((item) => (
    <a
      key={item.href}
      href={item.href}
      className="group flex flex-col gap-3 rounded-xl border border-border p-5 transition-colors hover:bg-muted"
    >
      <div className="flex items-center gap-3">
        <div className="flex size-9 items-center justify-center rounded-lg bg-secondary">
          <item.icon className="size-4 text-foreground" />
        </div>
        <h2 className="font-medium text-foreground">{item.title}</h2>
      </div>
      <p className="text-sm text-muted-foreground">{item.description}</p>
      <span className="mt-auto flex items-center gap-1 text-sm text-muted-foreground transition-colors group-hover:text-foreground">
        Read more <ArrowRight className="size-3" />
      </span>
    </a>
  ))}
</div>

## Core principle

Content in the Mukoko ecosystem is **clear, direct, and respectful**. We write for a diverse African audience — people who may speak English as a second or third language, who may be reading on a low-bandwidth connection, and who deserve the same quality of communication as users anywhere else in the world.
